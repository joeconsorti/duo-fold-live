# Release rotation override at fold endpoints

- Release the rotation hold immediately at the configured fully-open or fully-closed angle, without the former 450 ms settling delay or hardcoded 178-degree open endpoint.
- Restore fixed-to-user rotation policy before preference verification, so a failed preference readback does not leave that override in place.
- Preserve the working in-transition hold and short angle-gap protection. Animation rendering and handoff timing are unchanged.

Device verification required: auto-rotate should remain blocked during motion and resume at completion if it was enabled before the transition. Existing manual rotation lock should remain locked.
