# 3.0.4-alpha.11 — Repair Home-photo attachment

The alpha.10 handset trace showed an unattached Home anchor and hidden photo, with no completion callback after 53 seconds. That build never exercised the intended placement.

Use the existing authorized accessibility service connection directly, with an owned Binder callback and explicit interaction matching. Report connection/request/window identifiers, attachment success, rejection or the underlying exception. Stop waiting after five seconds, clean up failed surfaces, and retry at most three times per launcher window. Ignore duplicate, cancelled and late callbacks so they cannot activate or remove a live photo.

The existing renderer remains available on failure. Wallpaper placement still needs handset verification: visit Home after installation, then test unlock on each panel. The report must show “Home transition photo attached below launcher content” before assessing this placement. Capture another unlock trace if the flash persists.

Validation: attachment lifecycle regression tests and launcher selection tests; changed Java compilation locally. Full release tests, lint, assembly and signing run in CI. No animation or rotation changes.
