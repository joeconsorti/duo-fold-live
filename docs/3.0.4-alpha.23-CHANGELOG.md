# 3.0.4-alpha.23

- Retries animation-overlay exclusion when the Shizuku helper or overlay surface is not yet available at startup. Previously this was attempted once.
- Re-registers changed animation surfaces, drops destroyed surfaces, and delays mirror attachment until registered animation surfaces have been excluded.
- Retries failed live-preview attachment and rebuilds the mirror after new exclusions succeed. Retry work stops when its view is destroyed.
- Records exclusion retry counts in lifecycle diagnostics.

This addresses code-level startup races; confirm the original symptom after updating and rebooting with Shizuku available before promoting the build. Existing settings and animation appearance are unchanged.
