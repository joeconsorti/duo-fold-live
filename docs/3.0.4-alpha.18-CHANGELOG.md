# 3.0.4-alpha.18 — Anchor the cover animation corners

The reference projection scaled vertical coordinates against an offset model plane. At the fixed cover edge near closed, this sampled roughly 0.7–0.8% beyond the top and bottom image bounds, producing black inset wedges where the animation should meet the physical corners.

Normalize the cover's vertical projection by its fixed-edge perspective depth. Both fixed-edge corners stay at their original image coordinates while perspective variation across the moving panel remains. Canonical coordinates retain the existing rotation/reversal mapping. The inner-panel projection, Classic shader, crop, blur, wallpaper hold, rotation and alpha.17 readiness timings are unchanged.

Validation: numerical mapping checks at 11,001 hinge angles from 0–110 degrees and five vertical coordinates (55,005 checks); moving-edge projection remains finite. Full release unit tests, lint, Kotlin compilation, assembly and signing run in CI. The screenshot matches this projection defect; actual appearance still requires device confirmation.

Wallpaper flashing remains partly fixed. Investigate the saved Samsung wallpaper image-injection idea after this alignment work; it is not part of this build. Main/latest stays alpha.16 and the alpha.13 rollback release is preserved.
