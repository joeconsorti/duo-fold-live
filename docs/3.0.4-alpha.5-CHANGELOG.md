# 3.0.4-alpha.5 — inner reveal readiness test

- Windowed-glass inner reveal uses a transaction committed callback attached to the actual SurfaceView frame, rather than the parent UI draw.
- Normal glass readiness requires a matching, fresh content capture started after the destination panel was observed ON, followed by the glass-frame commit. Add a 2 ms minimum after that commit; visible changes remain refresh-paced.
- Fully-open fast unfolds accept a committed transparent endpoint frame instead of waiting for a glass effect that has already ended.
- Black fallback draws no longer report the prepared preview ready. Reject captures if the panel turns OFF during capture.
- Preserve the 900 ms emergency recovery and label it explicitly as readiness NOT confirmed.
- Preserve orientation locking, animation geometry/style, fade duration/smoothing, closing-mirror timing, and the alpha.4 sleep/health safeguards.

Software readiness is not proof that another app finished its inner-screen layout or that pixels are optically visible. The fully-open path confirms Duo cleared its own frame, not the underlying app. Test slow and fast unfolds in both orientations and copy the Handoff fade / Bridge trace lines if blank content remains. Phone verification is required.

Validation: 27 focused JVM tests passed locally. Full Android compile, unit tests, lint and signing run in GitHub Actions.
