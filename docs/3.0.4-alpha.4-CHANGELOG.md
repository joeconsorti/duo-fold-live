# Sleep/retry safeguards and earlier handoff reveal

- Prevent timed-out settings Binder calls from accumulating queued transactions on their single-thread worker.
- Back off failed keep-awake checks up to five minutes; skip checks while asleep. Keep-awake remains saved ON, with setting-change/wake checks and five-minute periodic verification while awake.
- Suspend angle-reader and recovery supervision after sustained screen-off (three-second grace preserves brief display handoffs), resume on wake, and pace reader restart attempts to at most once per 30 seconds except explicit screen-on events.
- Add bounded five-minute health samples, up to 24 hours / 288 rows, to Copy status report. Includes app-process CPU time, managed/native heap, threads/FDs, battery temperature/charging and Android thermal state. No alarms, wake locks, screenshots or app content. Sleeping samples are opportunistic. These do not measure system/Samsung/Shizuku process memory or establish the overheating cause.
- Reduce the fixed destination-ON black settling wait from 120 ms to 32 ms; continue requiring a fresh destination draw, preserving fade duration and the physical-OFF guard. Samsung's physical panel-OFF interval is not changed.
- Confirmed alpha.3 orientation implementation is untouched.

The reported overheating is not yet proven resolved. Test under supervision, not unattended overnight charging. After a test, use Developer settings → Copy status report for the health history.
