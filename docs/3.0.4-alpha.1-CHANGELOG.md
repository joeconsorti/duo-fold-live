# Orientation hold test — 3.0.4-alpha.1

- Temporarily hold secondary inner-panel rotation during fold/unfold transitions.
- Preserve an active hold through brief angle-reading gaps (up to 1.5 seconds).
- Release after the near-open endpoint settles, independently of the animation threshold.
- Retain saved rotation preference recovery. Animation rendering and handoff timing are unchanged.

Java compilation and 18 focused tests passed locally. Device behavior is not yet verified.

Please test folding and unfolding with Samsung auto-rotate enabled, in portrait and landscape. Confirm rotation works normally again after completion. If the hold fails, include Copy connection report and the main status report (Rotation hold line).
