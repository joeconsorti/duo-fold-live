# 3.0.4-alpha.15 — Restore the alpha.13 baseline

Restore alpha.13 application source and tests exactly. Remove alpha.14's continuous lock-state polling, two-second unlock fallback, and transaction-commit/next-frame bridge removal. Retain the rollback documentation and increment version metadata so this recovery installs over alpha.14 without uninstalling.

## Report findings

Duo-unlock-trace-55876058.zip records eight temporary-photo show/remove intervals: 23, 27, 19, 28, 23, 20, 24 and 29 ms. Each removal follows alpha.14's Home commit + next frame path. No expiry explains those removals. The trace also records a longer wake-to-unlock interval of about 2.0 seconds: the bridge is shown and then removed 28 ms later. Thus the longer wait did not simply exhaust the temporary-photo timeout in that reproduction.

These markers describe submitted operations, not physical panel presentation. They make the changed handoff a regression suspect, not a proven cause of the reported large flicker after wallpaper application. The captured events do not isolate a photo replacement sufficiently to attribute that symptom. The previous two-second polling-window diagnosis was a hypothesis, not established by this report.

## Fresh investigation boundary

Start future changes from alpha.13. Keep its atomic Home-placement/bridge-hide handoff intact. Isolate long-wait observation from handoff behavior in separate experiments; do not repeat alpha.14's combined timing changes. If wallpaper replacement flicker persists on the restored baseline, capture replacement itself and inspect old/new photo buffers and launcher window switches independently of unlock.

Validation: application source and tests compared byte-for-byte with alpha.13; full release tests, lint, assembly and signing in CI. This restores known partly working behavior; the delayed-unlock flash remains unresolved.
