# Rotation settings access repair

Fix the settings-provider access failure reported in alpha.1 by reading saved rotation preferences through the shell settings command rather than a synthetic app ContentResolver. This uses the same command mechanism as the working keep-awake helper. Original preferences remain journaled before rotation changes. Restoration writes are checked by readback.

Preserve the meaningful exception instead of replacing it with its remote stack-trace cause. Animation rendering and display handoff timing are unchanged.

Compilation and 18 focused tests passed locally. Rotation behavior requires device verification: test both directions with auto-rotate enabled and confirm normal rotation returns afterward. Include the Rotation hold status if it fails.
